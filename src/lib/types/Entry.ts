// Temporary Entry object for Veteran Umas
export type Entry = {
  id: string;
  trainee: string;
  skills: string[];
  sparks: string[];
  notes?: string;
  favorite?: boolean;
  createdAt: number;
};
